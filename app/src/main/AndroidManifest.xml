<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MADAssignment2"
        tools:targetApi="31">


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".onBordingScreen1" />

        <activity android:name=".onBoardingScreen2" />

        <activity android:name=".onBoardingScreen3" />

        <activity android:name=".Homepage" />

        <activity android:name=".Login" />

        <activity android:name=".Homepage_after_login" />

        <activity android:name=".User_profile" />

        <activity android:name=".Speaking_practice" />

        <activity android:name=".Product" />

        <activity android:name=".Message" />

        <activity android:name=".Find_friends" />




        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>